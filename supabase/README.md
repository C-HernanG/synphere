# Supabase Workspace

This directory is reserved for Synphere database migrations, local seed data, and Supabase-specific project configuration.

Expected contents over time:

- `migrations/`: SQL migrations
- `seed/`: local development seed data
- local CLI configuration if Supabase CLI is adopted later
