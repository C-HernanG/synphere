{
  "name": "synphere",
  "private": true,
  "version": "0.1.0",
  "description": "Open source social music discovery with Bubbles, taste Spheres, and real-time listening Rooms.",
  "license": "AGPL-3.0-only",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @synphere/web",
    "build": "npm run build --workspace @synphere/web",
    "typecheck": "npm run typecheck --workspace @synphere/web"
  }
}
